0.01
    [] Properly make parser resumable so that chunks can be processed
    [] Add support for true, false, and null
    [] Add numerical parsing instead of just slurping numbers as a string
    [] Write a serializer
    [] Use memory pools instead of direct allocation to decrease memory usage and remove memory leaks
    [] Fix all of the various warnings in the XML parser code
    [] Properly use the hashing library as an imported library rather than dumping
       all of the code in. ( shared todo with xml-bare )